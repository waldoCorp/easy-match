/**
 * Trigger function to automatically run partner_pair_trigger_function
 * to ensure that doubles are always kept in the partner table
**/

CREATE TRIGGER partner_pair_trig
	AFTER INSERT ON partners
	FOR EACH ROW
	EXECUTE PROCEDURE partner_pair_insert();

